{
  "name": "collatz",
  "version": "1.0.0",
  "description": "TypeScript program that computes the maximum Collatz sequence length.",
  "author": "Guilherme Lima Gonçalves <guligon90>",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "prebuild": "rimraf build",
    "build": "tsc",
    "validate": "tsc --noEmit",
    "dev": "ts-node-dev src/index.ts",
    "start": "yarn prebuild && yarn build && node build/index.js"
  },
  "dependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/node": "^18.6.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0"
  }
}
